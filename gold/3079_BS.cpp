// 매개변수 탐색 이용.
#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    long long n, m;
    cin >> n >> m;
    vector<long long> times(n);

    for(int i = 0; i<n; ++i){
        cin >> times[i];
    }


}

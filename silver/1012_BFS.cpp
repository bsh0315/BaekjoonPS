#include<bits/stdc++.h>
using namespace std;

int t;
int m, n, k;


int main(){
    cin >> t;

    while(t--){
        int arr[51][51] = {0,};
        cin >> m >> n >> k;
        for(int i = 0; i<k; ++i){
            int x, y;
            cin >> x >> y;
            arr[x][y] =1;
        }

        for(int i = 0; i<)
    }
}
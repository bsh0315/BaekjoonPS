#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int K = 0 , N = 0;
    cin >> K >> N;
    vector<int> arr(K);

    for(int i = 0; i<K; ++i){
        cin >> arr[i];
    }
    sort(arr.begin(), arr.end());

    
}
//단어 정렬
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct str{
    char arr[51];
}str;

void sorting(str *);

int main(){
    int N;
    scanf("%d", &N);

    str *ptr = (str *)malloc(sizeof(str)*N);

    
    

    return 0;
}